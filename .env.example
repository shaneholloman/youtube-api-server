# YouTube API Server Environment Configuration
# Copy this file to .env and fill in your actual values

# ===========================================
# WEBSHARE PROXY CONFIGURATION (RECOMMENDED)
# ===========================================
# These credentials are used to bypass YouTube's IP blocking
# Sign up at https://www.webshare.io/ and get residential proxy package
# Find your credentials in: https://dashboard.webshare.io/proxy/settings

WEBSHARE_PROXY_USERNAME=your_webshare_username_here
WEBSHARE_PROXY_PASSWORD=your_webshare_password_here

# Optional: Limit proxy IPs to specific countries (comma-separated)
# Example: WEBSHARE_PROXY_LOCATIONS=us,de,gb
# WEBSHARE_PROXY_LOCATIONS=

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Host and port for the FastAPI server
HOST=0.0.0.0
PORT=8000

# ===========================================
# USAGE INSTRUCTIONS
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual Webshare credentials
# 3. Start the server: python main.py
#
# Without proxy credentials, the server will work but may face IP blocking
# from YouTube after making several requests.
#
# To test if your proxy is working:
# curl http://localhost:8000/health
